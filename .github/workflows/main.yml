on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      -name: "login to Docker Hub"
        run: docker login --username mohamedkonsowa --password dckr_pat_ibHxafDk56T-unDfpLQxhSb296s
